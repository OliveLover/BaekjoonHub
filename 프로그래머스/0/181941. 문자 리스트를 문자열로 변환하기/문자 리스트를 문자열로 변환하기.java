class Solution {
    public String solution(String[] arr) {
        String answer = "";
        int arrSize = arr.length;
        
        for (int i = 0; i < arrSize; i++) {
            answer += arr[i];
        }
        return answer;
    }
}